<%= form_for(@producto_detail) do |f| %>
  <% if @producto_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@producto_detail.errors.count, "error") %> prohibited this producto_detail from being saved:</h2>

      <ul>
      <% @producto_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :producto_id %><br />
    <%= f.number_field :producto_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
